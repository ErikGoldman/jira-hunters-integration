{
  "scripts": {
    "dev": "set -a && source local-env-vars.txt && set +a && node -r ts-node/register src/index.ts",
    "debug": "set -a && source local-env-vars.txt && set +a && node --inspect-brk -r ts-node/register src/index.ts"
  },
  "dependencies": {
    "@types/jira-client": "^7.1.4",
    "jira-client": "^8.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
